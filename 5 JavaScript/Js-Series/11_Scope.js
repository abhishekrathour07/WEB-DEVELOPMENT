
if(true){
let a =20;
const b=30;
var c =120;
}
// console.log(a);
// console.log(b);
console.log(c);

// the variable which is declare inside the any block is known local scope .the access level of that variable is only inside that block
// the varible which is accessible by anywhere is callled global  variable
// var keyword doesn't follow this rule that's why we cant use the var keyword .reinitalised bhi ho jata hai ye without error