#include <stdio.h>

void main(){
    char str[20];
    int i;
    printf("Enter string char:");
    gets(str);
    int n =  printf("%s\n",str);
    printf("Length = %d",n);
    // for(i=0; str[i]!='\0'; i++);
    // printf("length = %d",i);

}
    